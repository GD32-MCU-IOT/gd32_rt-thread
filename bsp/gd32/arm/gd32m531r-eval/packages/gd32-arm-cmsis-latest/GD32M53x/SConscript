import rtconfig
from building import *

# get current directory
cwd = GetCurrentDir()

# The set of source files associated with this SConscript file.
src = Split('''
GD/GD32M53x/Source/system_gd32m53x.c
''')

path = [
    cwd + '/GD/GD32M53x/Include',
    cwd,
]

if rtconfig.PLATFORM in ['gcc']:
    src += ['GD/GD32M53x/Source/GCC/startup_gd32m53x.S']
elif rtconfig.PLATFORM in ['armcc', 'armclang']:
    src += ['GD/GD32M53x/Source/ARM/startup_gd32m53x.s']
elif rtconfig.PLATFORM in ['iccarm']:
    src += ['GD/GD32M53x/Source/IAR/startup_gd32m53x.s']

CPPDEFINES = ['USE_STDPERIPH_DRIVER']

group = DefineGroup('Libraries', src, depend = ['SOC_SERIES_GD32M53x'], CPPPATH = path, CPPDEFINES = CPPDEFINES)

Return('group')
