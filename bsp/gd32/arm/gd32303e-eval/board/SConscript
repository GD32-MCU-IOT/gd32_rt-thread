import os
import rtconfig
from building import *

cwd = GetCurrentDir()

# add general drivers
src = Split('''
board.c
board_msd_init.c
''')

# Add DMA peripheral source for GD32F30x (needed by UART/SPI/I2C DMA drivers)
src += [cwd + '/../packages/gd32-arm-series-latest/GD32F30x/GD32F30x_standard_peripheral/Source/gd32f30x_dma.c']

path =  [cwd]

CPPDEFINES = ['GD32F30X_HD']
group = DefineGroup('Drivers', src, depend = [''], CPPPATH = path, CPPDEFINES = CPPDEFINES)

Return('group')
