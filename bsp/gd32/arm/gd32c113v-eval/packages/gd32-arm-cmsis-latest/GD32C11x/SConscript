import rtconfig
from building import *

# get current directory
cwd = GetCurrentDir()

# The set of source files associated with this SConscript file.
src = Split('''
GD/GD32C11x/Source/system_gd32c11x.c
''')

path = [
    cwd + '/GD/GD32C11x/Include',
]
if rtconfig.PLATFORM == 'gcc':
    path += [cwd]

if rtconfig.PLATFORM in ['gcc']:
    src += ['GD/GD32C11x/Source/GCC/startup_gd32c11x.S']
elif rtconfig.PLATFORM in ['armcc', 'armclang']:
    src += ['GD/GD32C11x/Source/ARM/startup_gd32c11x.s']
elif rtconfig.PLATFORM in ['iccarm']:
    src += ['GD/GD32C11x/Source/IAR/startup_gd32c11x.s']

CPPDEFINES = ['USE_STDPERIPH_DRIVER', 'GD32C113']

group = DefineGroup('Libraries', src, depend = ['SOC_SERIES_GD32C11x'], CPPPATH = path, CPPDEFINES = CPPDEFINES)

Return('group')
